services:
  docs:
    image: learnxy_backend_local_docs
    container_name: learnxy_backend_local_docs
    build:
      context: .
      dockerfile: ./compose/local/docs/Dockerfile
    env_file:
      - ./.envs/.local/.django
    volumes:
      - /app/.venv
      - ./docs:/docs:z
      - ./config:/app/config:z
      - ./learnxy_backend:/app/learnxy_backend:z
    ports:
      - '9000:9000'
    command: /start-docs
